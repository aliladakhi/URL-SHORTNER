<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <% users.forEach(user=>{%>
      <ul>
        <li><%=user.shortUrl %></li>
        <li><%=user.redirectUrl %></li>
        <li><%=user.totalVisits.length==0?"[Not Yet Visited]":user.totalVisits %></li>
      </ul>
  <%})%>
</body>
</html>